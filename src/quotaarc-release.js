// Configure these values only after publishing a real public QuotaArc release.
// `url` must point to one GitHub Release asset, not to a repository or tag page.
export const quotaarcRelease = Object.freeze({
  version: "",
  url: ""
});
